/**
 * The "Ask about my work" agent endpoint (see /ask-agent in this repo).
 *
 * undefined -> the 🔮 plaque is hidden entirely.
 * After deploying ask-agent on Coolify, set this to its URL, e.g.
 *   export const ASK_ENDPOINT: string | undefined = 'https://ask.46-225-235-124.sslip.io';
 */
export const ASK_ENDPOINT: string | undefined = undefined;
